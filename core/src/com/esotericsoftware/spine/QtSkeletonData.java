/* package com.esotericsoftware.spine;

public class QtSkeletonData extends SkeletonData {

    private final SkeletonBinary skeletonBinary;

    public QtSkeletonData(SkeletonData skeletonData, SkeletonBinary skeletonBinary) {
        super.name = skeletonData.name;
        super.defaultSkin = skeletonData.defaultSkin;
        super.width = skeletonData.width;
        super.height = skeletonData.height;
        super.version = skeletonData.version;
        super.hash = skeletonData.hash;
        super.imagesPath = skeletonData.imagesPath;
        this.skeletonBinary = skeletonBinary;
    }

    public SkeletonBinary getSkeletonBinary() {
        return skeletonBinary;
    }
} */